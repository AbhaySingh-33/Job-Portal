node_modules/
.next/
dist/
build/
.env
.env.local
*.log
.DS_Store

# Docker files
Dockerfile
docker-compose.yml
docker-compose.yaml

# Track project folders in monorepo
# (removed ignoring of frontend/ and services/)
# If you want to exclude build outputs inside them, use nested .gitignore files.
